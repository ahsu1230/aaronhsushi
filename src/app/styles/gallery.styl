@import "constants"

#view-gallery
	& .column
		display: inline-block
		width: 33.33%
		vertical-align: top

@keyframes pulse
	0% { transform: scale(0); }
	85% { opacity: 1; }
	100% { transform: scale(1); filter: blur(2px); opacity: 0; }